export declare enum OrderStatus {
    Created = "created",//order has been created, ticket try to order is not reserved
    Cancelled = "cancelled",// ticket the order is trying to reserve is already reserved, order cancelled, or order expires before paymemt expiry time
    AwaitingPayment = "awaiting:payment",//order successfully reserved the ticket
    Complete = "complete"
}
